spring:
  application:
    name: wallet
  server:
    port: 8080
    servlet:
      context-path: /wallet
  datasource:
    url: jdbc:mysql://localhost:3306/my_wallet
    username: root
    password: root&3333fgdsdcggf
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      database-platform: org.hibernate.dialect.MySQLDialect
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
    sql:
      init:
        mode: always

